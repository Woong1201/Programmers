def solution(n, start, end, roads, traps):
    roads.sort()
    step = []
    
    for i in range(len(roads)) :
        if roads[i][0] == start:
            while roads[i][0] == start:
                step.append(roads[i][1])
        break
    
    while True :
        
        
        
    
    return 1


def maze(path):
    
    
    
    
    return 1


import copy
n = 4; start = 1; end = 4;
roads = [[1, 2, 1], [3, 2, 1], [2, 4, 1]]
path = [roads]
traps = [2, 3]
roads1 = copy.deepcopy(path)
roads1[0][0][0] = 0
roads
roads1
roads.sort()
roads
